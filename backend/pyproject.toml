[project]
name = "cqvip"
version = "0.1.0"
description = "Computerized Quality Validation Information Platform"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
